<!DOCTYPE html>
<html lang="en">
<head>
</head>
<body>
    <center>
        <h1> Search Engine </h1>
        <form action="/search" method="get">
            <input type="text" name="query" />
            <input type="submit" value="Search" />
        </form>
        <form action="list-of-pokemons" method="get">
            <input type="submit" value="Search all">
        </form>
    </center>
    <%
            if(results){
                if(list.length > 0){
        %>
        <b>Resultados da pesquisa por <%= search %>:</b>
        <ul>
            <% list.forEach(function(pokemon){ %>
                <li><%= pokemon.name %> (<%= pokemon.type %>)</li>
            <% }); %>
        </ul>
        <% } else{ %>
            <b>Nenhum resultado encontrado para <%= search %>!</b>
        <% 
            }
        }
        %>
</body>
</html>